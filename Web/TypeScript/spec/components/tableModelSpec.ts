import { Model as TableModel } from '../../components/table';

describe('TableModel', () => {
    describe('load', () => {

        it("should load table", function (done) {
            done();
        });
    });
});
