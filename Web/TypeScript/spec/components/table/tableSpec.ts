import { Model } from '../../../components/table';

describe('Model', () => {
    describe('load', () => {

        it("should load table", function (done) {
            done();
        });
    });
});
