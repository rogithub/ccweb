import { Request } from '../../services/request';

describe('Component', () => {
    let service: Request;

    beforeEach(() => {
        service = new Request();
    });

    describe('get', () => {

        it("should fetch data", function (done) {
            done();
        });
    });
});
